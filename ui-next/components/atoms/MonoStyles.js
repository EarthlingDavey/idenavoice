import styled from 'styled-components';

const MonoStyles = styled.span`
  font-family: 'Lucida Console', Monaco, monospace;
  color: #ddd;
  overflow-x: auto;
  font-size: 11px;
`;

module.exports = {
  MonoStyles,
};
